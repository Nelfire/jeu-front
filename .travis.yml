  
language: node_js
node_js:
  - "12"
before_script:
  - npm install
script: 
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: dist/front
  on:
    branch: master
notifications:
  email: false